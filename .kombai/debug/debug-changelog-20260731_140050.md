## Changes Made

### 1. Disabled automatic leadership rotation on mobile
- **File:** `components/sections/home/leadership-section.tsx`
- **Change:** The profile rotation effect now exits when `mobile` is true and includes `mobile` in its dependency list.
- **Why:** Mobile rendering uses the reduced layout path, but the two-second profile interval still changed normal-flow copy height. That moved the sections below leadership by about 51px as profiles cycled.
- **Revert:** Restore the guard to `if (prefersReducedMotion) return;` and the dependency list to `[prefersReducedMotion]`.

## Revert Status
- [ ] Change 1 - Mobile leadership rotation guard
